Da bi omogucio korišćenje skriprti potrebno je izvršiti setup.bash skriptu pokretanjem sledeće naredbe:

        ./setup.bash

Ovo bi trebalo da registuruje komande, ali ako to ne uspe pokrenuti:

        source ~/.m2_custom_commands.sh 